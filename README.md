# Web Fetch Agent

An AI agent that fetches and summarizes web content built with WSO2 Integrator: BI.

## Overview

This chat service provides an HTTP endpoint that accepts URLs and returns concise summaries of the retrieved content. It uses the Ballerina AI module with MCP (Model Context Protocol) to fetch web pages and generate neutral, clear summaries.
